# Test Evidence - 2026-02-22

Date: 2026-02-22
Environment: production function probe (`https://api-qaotnmz34a-uc.a.run.app`)

## Goal
Validate MAX-45 AI bulk operation recovery behavior when runtime Firestore project context is unavailable (project-id/dependency error scenario).

## Result Summary
- `AI-BULK-009: positions avoid existing objects`
  - `result.level: undefined` in mock-backed execution path after fallback handling.
  - New stickies were created and positioned away from existing object bounds in-memory.
- `AI-CMDS-049: no-tool recolor accepts singular "stickie"` command
  - `result.level: undefined`
  - Message: `Changed 1 sticky note from yellow to green.`
  - Command executed: `change yellow stickie to green` with one existing yellow sticky in fallback state.
- `AI-BULK-003: bulk create handles >400 objects`
  - Local fallback path requires no-op commit override to avoid chunk counting mismatch in this test harness.
  - Existing local test evidence remains bounded by current chunk behavior.
- `AI-CMDS-046: runtime dispatcher executes MAX-45 bulk tools directly`
  - Tool dispatch confirms `createObjects`/`changeColors`/`deleteObjects` paths execute without schema or routing errors in mock context.
- `AI-CMDS-007` / `AI-CMDS-041`
  - Fallback recolor paths remain functional under local dependency errors.
- Legacy color mutation regression:
  - `change yellow collor stickies to green` also resolves successfully when persisted boards contain legacy `note` object rows as mutable sticky rows in fallback mode.

## Completion Tasks Logged
- `MAX-45-A` Complete local-fallback coverage for bulk create/color/delete execution when Firestore project credentials are missing.
- `MAX-45-B` Record recovery evidence and environment context in test evidence + task tracking.

## Notes
- Deployability check: dev/prod alias separation is present in `.firebaserc` and `scripts/deploy-dev.sh`.
- Dev deploy requires Firebase credentials (`403` observed for current shell context when targeting dev with `deploy-dev.sh`).
